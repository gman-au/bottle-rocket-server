@page "/MyWorkflow/Dropbox/{workflowId}/AddStep"
@page "/MyWorkflow/Dropbox/{workflowId}/Steps/{parentStepId}/AddStep"
@page "/MyWorkflow/Dropbox/{workflowId}/Steps/{stepId}/UpdateStep"
@using Rocket.Domain.Core.Utils
@using Rocket.Dropbox.Contracts
@inherits Rocket.Web.Host.Infrastructure.BaseSecureCancellableComponent

<UpdateWorkflowStepWrapper
    T="DropboxUploadWorkflowStepSpecifics"
    WorkflowStepName="Upload file to Dropbox"
    WorkflowId="@WorkflowId"
    ParentStepId="@ParentStepId"
    RequiresConnectorCode="@DomainConstants.ConnectorCodeDropboxApi"
    StepId="@StepId"
    Context="stepSpecifics">
    <MudGrid>
        <MudItem sm="12" lg="6">
            <MudTextField
                T="string"
                Label="Subfolder"
                HelperText="Enter the Dropbox sub folder to upload to"
                @bind-Value="stepSpecifics.Subfolder"
                For="@(() => stepSpecifics.Subfolder)"/>
        </MudItem>
    </MudGrid>
</UpdateWorkflowStepWrapper>

@code {

    [Parameter]
    public string WorkflowId { get; set; }

    [Parameter]
    public string ParentStepId { get; set; }

    [Parameter]
    public string StepId { get; set; }

}