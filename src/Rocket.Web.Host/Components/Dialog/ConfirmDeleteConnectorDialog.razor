@using Microsoft.Extensions.Localization
@using Rocket.Api.Contracts.Connectors
@using Rocket.Localization.Web
@inject IStringLocalizer<DialogResource> StringLocalizer

<MudDialog Class="p-2">
    <TitleContent>
        <MudText
            Color="Color.Primary"
            GutterBottom
            Typo="Typo.h5">
            @StringLocalizer["DialogDeleteConnectorTitle"]
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudText
            Typo="Typo.body1"
            Color="Color.Default">
            @StringLocalizer["DialogDeleteConnectorBody"]
        </MudText>
        <MudText
            Class="mt-2 mb-2"
            Typo="Typo.h6"
            Color="Color.Info">
            @Connector?.ConnectorName
        </MudText>
        <MudText
            Typo="Typo.body1"
            Color="Color.Warning"
        >
            @StringLocalizer["DialogDeleteConnectorWarning"]
        </MudText>
    </DialogContent>
    <DialogActions>
        <MudButton
            Variant="Variant.Outlined"
            OnClick="Cancel">
            @StringLocalizer["DialogDeleteConnectorCancelCaption"]
        </MudButton>
        <MudButton
            Variant="Variant.Outlined"
            Color="Color.Error"
            OnClick="Submit">
            @StringLocalizer["DialogDeleteConnectorSubmitCaption"]
        </MudButton>
    </DialogActions>
</MudDialog>

@code {

    [CascadingParameter]
    private IMudDialogInstance MudDialog { get; set; }
    
    [Parameter]
    public ConnectorSummary Connector { get; set; }

    private void Submit() => MudDialog.Close(DialogResult.Ok(true));

    private void Cancel() => MudDialog.Cancel();

    protected override async Task OnParametersSetAsync()
    {
        await
            MudDialog
                .SetOptionsAsync(
                    new DialogOptions
                    {
                        FullWidth = true,
                        MaxWidth = MaxWidth.Small,
                        BackgroundClass = "dialog-blur-backdrop"
                    }
                );
    }

}