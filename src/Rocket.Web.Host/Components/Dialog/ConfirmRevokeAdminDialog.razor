@using Microsoft.Extensions.Localization
@using Rocket.Localization.Web
@inject IStringLocalizer<DialogResource> StringLocalizer

<MudDialog Class="p-2">
    <TitleContent>
        <MudText
            Color="Color.Warning"
            GutterBottom
            Typo="Typo.h5">
            @StringLocalizer["DialogRevokeAdminTitle"]
        </MudText>
    </TitleContent>
    <DialogContent>
        <MudText
            Typo="Typo.h6"
            Color="Color.Default"
        >
            @StringLocalizer["DialogRevokeAdminBody"]
        </MudText>
        <MudText
            Typo="Typo.body1"
            Color="Color.Error">
            @StringLocalizer["DialogRevokeAdminWarning"]
        </MudText>
    </DialogContent>
    <DialogActions>
        <MudButton
            Variant="Variant.Outlined"
            OnClick="Cancel">
            @StringLocalizer["DialogRevokeAdminCancelCaption"]
        </MudButton>
        <MudButton
            Variant="Variant.Outlined"
            Color="Color.Error"
            OnClick="Submit">
            @StringLocalizer["DialogRevokeAdminSubmitCaption"]
        </MudButton>
    </DialogActions>
</MudDialog>

@code {

    [CascadingParameter]
    private IMudDialogInstance MudDialog { get; set; }

    private void Submit() => MudDialog.Close(DialogResult.Ok(true));

    private void Cancel() => MudDialog.Cancel();

    protected override async Task OnParametersSetAsync()
    {
        await
            MudDialog
                .SetOptionsAsync(
                    new DialogOptions
                    {
                        FullWidth = true,
                        MaxWidth = MaxWidth.Small,
                        BackgroundClass = "dialog-blur-backdrop"
                    }
                );
    }

}